<form method="post" action="{{ path('app_type_forum_delete', {'id': type_forum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_forum.id) }}">
    <button class="btn">Delete</button>
</form>
